/**
 * Shared library for the Sprintmodus backend services: tenant context, dynamic DataSource
 * routing and subscription limits. This is a plain library (no application class), consumed
 * as a Maven dependency by auth-service, project-service and workitem-service.
 */
package com.sprintmodus.common_lib;
